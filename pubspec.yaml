name: todo_list
description: A ToDO List Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.12
  flutter_local_notifications: ^12.0.4
  timezone: ^0.9.0 # Updated to ^0.9.0
  sensors_plus: ^4.0.2
  provider: ^6.0.5
  fl_chart: ^0.40.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
